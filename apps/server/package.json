{
  "name": "@macrofactor-oss/server",
  "private": true,
  "type": "module",
  "scripts": {
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev",
    "test": "vitest --run",
    "lint": "prettier . --write"
  },
  "dependencies": {
    "hono": "^4.9.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.66",
    "@cloudflare/workers-types": "^4.20250823.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.32.0"
  }
}
